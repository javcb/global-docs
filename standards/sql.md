# SQL standards

- save important SQL as .sql files
- prefer readable CTEs over overly compressed queries
- use parameterization where possible
- name files by purpose and target object
- separate reusable SQL from project-specific one-offs